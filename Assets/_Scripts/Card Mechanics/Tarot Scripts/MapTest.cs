using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapTest : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        print((TarotElement)TarotSuit.Cups);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
